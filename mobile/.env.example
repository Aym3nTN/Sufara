# Point the app at a specific API. Omit it and the app falls back to port 4000
# on the Metro host, which is what makes "open on my phone" work unconfigured.
EXPO_PUBLIC_API_URL=http://localhost:4000/api/v1
